feat(paw-shield): integracion Petify completa + opt-in con dedup + estrategia B2B

Reemplaza el pipeline propio (DINOv2/SigLIP2 ~random) con Petify (PetNow)
B2B API V2. Test interno con 8 mascotas reales:
  - 1 foto por pet  → 75% top-1 accuracy
  - 3 fotos por pet → 100% top-1 accuracy con scores 96-100

Modelo de negocio: Opcion B "Paw Shield activable"
  - Pet creado por defecto SIN Petify (default OFF en flag y schema).
  - Solo el dueño que quiere proteccion contra extravio activa
    explicitamente desde la ficha de la mascota.
  - ~25-30% activacion estimada → reduce 70-75% costo SaaS-per-pet.
  - Politica anti-explosion de costos documentada en
    docs-raiz/PAW_SHIELD_PLAYBOOK.md.

DB · supabase/migrations/20260910000000_paw_shield_petify.sql
  - pets: petify_pet_id, petify_registered_at, petify_fingerprint_count,
    petify_quality, nose_print_pending.
  - UNIQUE INDEX en petify_pet_id (anti-duplicado: 1 mascota = 1 ficha).
  - Tabla audit `paw_shield_events` para troubleshooting.
  - Tabla nose_print_embeddings legacy PRESERVADA por 6 meses.

Edge functions
  - _shared/petify-client.ts: cliente Deno reutilizable (V2 API).
    createCaptureSession, uploadFingerprint, createPet, deletePet,
    addFingerprintsToPet, identifyPets, pollJob + 2 helpers compuestos.
  - paw-shield-register: dedup server-side ANTES de crear (identify >=92
    score con gap >=5 → duplicate_detected; gap <5 → ambiguous_match).
  - paw-shield-identify: publico (verify_jwt=false). Cruza petify_pet_id
    contra nuestra tabla pets, devuelve top-3 con info publica.

Frontend
  - lib/videoFrameExtractor.ts: extractFramesAtTimes + measureSharpness
    (Laplacian variance) para calidad de captura sin modelos pesados.
  - components/paw-shield/PawShieldEnrollment.tsx: video 3 seg →
    extract 3 frames → quality check → upload. 3 reintentos antes de
    rendirse (marca pet como nose_print_pending para nudge en /home).
  - components/paw-shield/PawShieldStatusCard.tsx: tarjeta en tab
    Identidad. 4 estados visuales (sin activar / pending retry /
    low quality / high quality).
  - PetClinicalRecord/index.tsx: monta PawShieldStatusCard cuando flag.
  - NoseScan.tsx: switch backend a paw-shield-identify (mantiene UI).

Feature flag
  - PAW_SHIELD_PETIFY: false hasta firmar contrato Petify.
  - NOSE_PRINT_ENABLED: comentario actualizado (reemplazado).

Test scripts (gitignored runtime, no commit)
  - scripts/test-petify.mjs (single foto por pet)
  - scripts/test-petify-multi.mjs (3 fotos por pet) — el que valido 100%

Estrategia comercial · docs-raiz/PAW_SHIELD_PLAYBOOK.md
  - Anti-explosion costs (default OFF, cleanup automatico, alerts).
  - Features que aprovechan IDENTIFY ILIMITADO:
    - Dueno: onboarding express, login del pet, quick switcher
    - Vet: check-in biometrico, medication match-back
    - Refugios: anti-robo, pre-adopcion, follow-up
    - Tiendas: scan-to-discount + data de "que compra que perro"
    - Lost-and-found pasivo en feed social
    - Cross-app interop (smart pet doors, edificios pet-friendly)
  - Data layer: pharma dashboards, insurance actuarial, retail recs,
    municipios Ley 21.020, public insights SEO.
  - Promociones dirigidas: birthday + alimento por raza/edad +
    vacuna venciendo + cambio comuna + hermanos geneticos.
  - Riesgos + mitigaciones + metricas 90d.

Verificacion
  - npx tsc -b: 0 errores con strict: true
  - npx vitest run: 603/603 verde
  - Pendientes Pedro: aplicar SQL + redeploy edge fns + flag ON post-Petify

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

# Paw Friend — Contexto al 2026-04-07 (post sesión auditoría + rediseño Joao)

> Documento maestro de estado para arrancar la próxima sesión sin tener que recordar nada. Reemplaza a `ESTADO_APP.md` (archivado en `_archive/`).

---

## 1. Identidad del producto (sin cambios)

- **Nombre**: Paw Friend  
- **Dominio**: pawfriend.cl (GitHub Pages, deploy desde `docs/`)
- **Repo**: github.com/pedrosusaetahidalgo-sudo/pet-harmony-chile, branch `main`
- **Supabase project**: `gwailbjlvevkhwcrovfd`
- **Mobile**: Capacitor (Android compilable, iOS testeado en simulator)
- **B2C dueños**: gratis. **B2B vets**: planes pagos (motor de ingresos).
- **Joya de la corona**: ficha médica descargable + directorio público de vets. **No tocar**.

---

## 2. Estado técnico actual (verificado en esta sesión)

| Gate | Resultado |
|---|---|
| `npx tsc -b` | ✅ 0 errores (105 → 0 en la auditoría) |
| `npm run build` | ✅ pasa, ~58s |
| Bundle principal | 501 kB / 144 kB gzip |
| Migraciones aplicadas en DB remota | ✅ todas hasta `20260411000000` |
| Buckets Storage | ✅ 5/5 con policies (`avatars`, `pet-photos`, `walk-photos`, `medical-documents`, `verification-docs`) |
| FK `post_comments → profiles` y `service_reviews → profiles` | ✅ creadas en migración `20260409000000` |
| Tabla `ai_request_quota` + RPC | ✅ rate limiting activo en 5 edge functions |
| Tabla `pet_activities` + `pet_activity_cheers` + trigger | ✅ activas |
| Edge functions (8 activas) | ✅ deployadas con `claude-sonnet-4-5` (model ID corregido) |
| Spend cap Anthropic | ⚠️ pendiente: configurar en https://console.anthropic.com/settings/limits |

---

## 3. Cambios estructurales recientes

### 3.1. Auditoría funcional end-to-end (commits `0293e53` → `9168180`)
- Barrido horizontal de 55+ archivos: `console.*` → `logger.*`, `error.message` crudo → `describeSupabaseError(error)`.
- Helper nuevo `src/lib/supabaseErrors.ts` con traducción de códigos Postgres a español.
- 64/65 tablas frontend ↔ DB cruzadas. La 65ª (`avatars`) era bucket Storage, no tabla.
- Bug runtime crítico transversal: `sb undefined` en 6 hooks/páginas (alias huérfano de un refactor previo).
- Dominio 1 (`pets`) y Dominio 2 (`medical_records`) reparados a fondo: insert+select atómico, `owner_id` explícito, user guards.

### 3.2. Edge functions (commits `4dee263`, `74ff92b`, `aca6150`, `ce90b11`)
- **Bug crítico fixed**: las 6 functions usaban `claude-sonnet-4-5-20241022` que no existe → todas tiraban 500. Ahora usan `claude-sonnet-4-5`.
- `max_tokens` reducidos: `breed-tips` 1500→800, `medical-suggestions` 2000→1000, `moderate-service-promotion` 1000→400.
- **Rate limit por usuario**: tabla `ai_request_quota` + RPC `check_and_increment_ai_quota`. Ventana rodante 1h. Límites:
  - `medical-suggestions` / `breed-tips` / `moderate-service-promotion`: 30/h
  - `pet-assistant`: 60/h
  - `generate-shelters`: 10/h (caro)
- 5 functions deployadas con `npx supabase functions deploy ... --project-ref gwailbjlvevkhwcrovfd`.
- `_shared/ai-base.ts` existe pero está huérfano (ningún caller real). Si se usa en el futuro, NO duplicar el rate limit.

### 3.3. Rediseño Joao — 5 fases (commits `931fedd` → `b80e330`)
- **Fase 1 — Home dashboard**: refactor de `src/pages/Home.tsx` a status cards 2x2 mobile / 4x1 desktop. Componente `src/components/home/StatusCard.tsx`. Eliminado hero grande, paragraphs, widgets duplicados.
- **Fase 2 — `useOrganicRewards`**: hook fire-and-forget en `src/hooks/useOrganicRewards.ts`. Integrado en `AddPet`, `AddMedicalRecord`, `CreateReviewForm`. Toasts discretos, 0 modales bloqueantes.
- **Fase 3 — Social tipo Strava**: tabla `pet_activities` + `pet_activity_cheers` con trigger. Componente `src/components/social/ActivityFeed.tsx`. Página `/actividad`. Slot integrado en Home.
- **Fase 4 — Servicios consolidados**: ya estaba hecho (`/services/:type` con redirects legacy).
- **Fase 5 — Reporte**: `REDESIGN_REPORT.md` con preguntas abiertas sin responder.

### 3.4. Bugs runtime de la última iteración
- `PetCard.tsx`: queries `post_likes` con postId no-UUID (demo cards landing) → 400. Fixed con regex UUID guard (commit `18595ba`).
- Cache stale del `index.html` post-deploy → meta `no-cache` + `vite:preloadError` listener auto-reload (commit `d1e3c41`).
- `medical_records` constraint: `cirugia` (sin tilde) ≠ `cirugía` (con tilde). Ese y otros 3 dropdowns desalineados con CHECK constraints fixed en commits `faa2b68`, `eddba7e`, `5f0e977`, `003d1e6`.

---

## 4. Edge functions — inventario completo y uso

| Function | Llama Anthropic | Uso en frontend | Estado |
|---|---|---|---|
| `medical-suggestions` | ✅ | `AddMedicalRecord.tsx` (sugerencias por raza+tipo) | OK con rate limit 30/h |
| `breed-tips` | ✅ | Falta confirmar caller | OK con rate limit 30/h |
| `pet-assistant` | ✅ | Chat IA en `PetAssistant.tsx` (?) | OK con rate limit 60/h |
| `generate-shelters` | ✅ | Admin/seed | OK con rate limit 10/h |
| `moderate-service-promotion` | ✅ | `CreateServicePromotion.tsx` | OK con rate limit 30/h |
| `generate-medical-summary` | ❓ | Probable: ficha médica PDF | **No auditada esta sesión** |
| `generate-medical-zip` | ❌ | Bundle de docs médicos | **No auditada esta sesión** |
| `generate-sitemap` | ❌ | SEO sitemap dinámico | **No deployada** según ESTADO viejo |
| `webpay-confirm` | ❌ | Callback Transbank pagos | **No auditada esta sesión** |
| `_shared/ai-base.ts` | helper | huérfano (sin callers) | dejar como referencia |

**Pendiente fase próxima**: auditar `generate-medical-summary`, `generate-medical-zip`, `generate-sitemap`, `webpay-confirm`. Las primeras dos son críticas porque tocan la joya de la corona (ficha médica PDF).

---

## 5. Estado del modelo de datos (migraciones aplicadas)

### Migraciones en `supabase/migrations/`
- Hasta `20260408100000`: heredadas (esquema base + features previos).
- `20260409000000_fk_comments_reviews_to_profiles.sql` → FKs faltantes para que el embed `profiles(...)` funcione en `PostComments` y `ReviewsList`. **Aplicada**.
- `20260410000000_ai_request_quota.sql` → tabla + RPC de rate limiting. **Aplicada manualmente** (el push del SQL automático había fallado, se aplicó vía SQL Editor).
- `20260411000000_pet_activities_social.sql` → tabla `pet_activities` + `pet_activity_cheers` + trigger `bump_pet_activity_cheers`. **Aplicada manualmente**.

### Tabla con bug menor preexistente (no urgente)
- `training_reviews`: tiene RLS habilitado pero **sin policies** → nadie puede leer/escribir. La app no la usa todavía, no rompe nada hoy. Cuando se implemente el flujo de training reviews, hay que agregar policies.

### Buckets Storage sin file_size_limit (riesgo bajo)
- `walk-photos` y `verification-docs` tienen `file_size_limit = NULL` (ilimitado). Riesgo: un user malicioso puede subir archivos enormes. Fix opcional:
  ```sql
  update storage.buckets set file_size_limit = 5242880 where id in ('walk-photos','verification-docs');
  ```

---

## 6. Preguntas abiertas del rediseño Joao (sin responder)

Documentadas en `REDESIGN_REPORT.md` § 6:

1. **Privacidad del feed**: hoy v1 = todos los users autenticados ven todas las activities. ¿OK o limitar a seguidores desde día 1?
2. **Cálculo de % completitud de ficha**: hoy 10 campos básicos de `pets`. ¿Ponderar distinto? ¿Incluir count de medical_records?
3. **Estado de vacunas**: hoy se deriva de `pet_reminders.type='vaccine'`. ¿Cambiar a `medical_records` (última vacuna real aplicada) como fuente de verdad?
4. **Racha de paseos**: lee `user_stats.streak_days` que nadie incrementa porque no hay flujo "log walk". ¿Crear botón "registrar paseo" en Home?
5. **Reward `walk_logged`**: hook listo sin caller. Idem.
6. **Toasts de reward**: hoy dicen literal "+5 puntos". ¿Más sobrio (solo "Paseo registrado") o así está bien?

---

## 7. Lo que el usuario quiere implementar a continuación (input para próxima sesión)

### 7.1. Premium para usuarios con más de 1 mascota (B2C monetización)
- **Cambio de modelo**: hoy B2C es 100% gratis. Ahora se introduce un freemium: 1 mascota gratis, 2+ requiere suscripción.
- **Decisiones a tomar antes de implementar**:
  - Precio mensual vs anual (sugerencia: $2.990 mensual o $24.990 anual chileno)
  - ¿Plan paga por mascota adicional o un único plan "multi-mascota"?
  - ¿Qué pasa con los users que YA tienen 2+ mascotas? Grandfathering vs forzar upgrade.
  - Pasarela de pago: Webpay Plus (ya integrado parcialmente) vs Stripe vs MercadoPago.
- **Tablas afectadas**: ya existen `subscriptions`, `profiles.premium_plan` (`monthly`/`yearly`), `payment_history`. Hay que verificar que el flujo end-to-end ande.
- **Frontend**: bloqueo en `AddPet` cuando el user ya tiene una mascota y no es premium. Toast claro + CTA "Suscribite". Nueva ruta `/upgrade` con planes y checkout.
- **Riesgo**: Joao y los vets pueden percibir esto como "se vendió a B2C" y romper el pitch B2B. Mensaje cuidadoso.

### 7.2. Rediseño del landing page (`/` = `Index.tsx`)
- **Problema**: hoy es muy largo, repite información, secciones descriptivas largas, parece scroll infinito. Mismo bug que tenía el Home antes del rediseño Joao.
- **Objetivo**: 1 hero claro + 1 prueba social + 1 bloque de beneficios concretos + 1 CTA principal. Punto. Sin "pet social en acción" + "demo cards" + "stats" + "testimonios" + "FAQ" todo en una página.
- **Inspiración**: landings tipo Linear, Strava, Cal.com — mucho whitespace, copy directo, máximo 4 secciones above-the-fold + 2 below.
- **No tocar**: `/para-veterinarios` (landing B2B) — esa funciona.
- **Output esperado**: bajar de los ~700 líneas actuales de `Index.tsx` a ~250 líneas con secciones bien definidas.

### 7.3. Auditar las 4 edge functions pendientes
Listadas en sección 4. Prioridad alta porque `generate-medical-summary` es la que produce el PDF que es la joya de la corona.

### 7.4. Recomendaciones de producto para reuniones con vets
Pedro tiene reunión con (a) una veterinaria establecida y (b) un grupo de vets recién egresados. Necesita:
- **Demo guion** (ya existe `DEMO_GUIDE.md`, hay que actualizarlo con los cambios del rediseño).
- **Pitch corto B2B** distinto al B2C.
- **Pricing claro** (Individual $9.900 / Clínica $29.900 / Pro $59.900).
- **Material para dejar después de la reunión**: PDF resumen o link a `/para-veterinarios` con landing optimizada.
- **Track de quién mostrar primero**: empezar por los recién egresados (más receptivos, sin clientela atada) y usar lo aprendido para la veterinaria establecida.

### 7.5. Afinar errores de backend
- Auditar las 4 edge functions pendientes (sección 4).
- Verificar que `webpay-confirm` funciona end-to-end (es crítico si se introduce el plan premium B2C).
- Revisar `training_reviews` (RLS sin policies).
- Revisar buckets sin size limit.
- Revisar logs de Supabase de los últimos 7 días buscando errores 500/400 recurrentes.
- Confirmar que las migraciones manuales (`20260410`, `20260411`) están reflejadas en el tracker de migraciones (`supabase_migrations.schema_migrations`).

### 7.6. Pendientes "para atrás" del ESTADO_APP.md original (verificar cuáles siguen aplicando)
1. ✅ ~~Verificar fix de auth en device real~~ — el barrido horizontal y los fixes de la auditoría tocaron Auth.tsx. Falta test en device físico iOS/Android.
2. ✅ ~~Aplicar 2 migraciones SQL pendientes~~ — hechas + 3 nuevas más.
3. ⏳ **Eliminar Edge Function `get-google-maps-key`** del Supabase Dashboard (sigue desplegada aunque el código no la use).
4. ⏳ **Eliminar variable `VITE_GOOGLE_MAPS_API_KEY`** del deploy y Google Cloud Console.
5. ⏳ **Configurar budget alert** en Google Cloud Billing en USD 1.
6. ⏳ **God component `PetClinicalRecord.tsx` (1444 líneas)** — solo se extrajeron types + helpers. Falta dividir las 5 funciones Tab (`TabResumen`, `TabHistorial`, `TabAlimentacion`, `TabDocumentos`, `TabCompartir`).
7. ⏳ **Migrar imports legacy a `@/lib/icons` y `@/lib/links`** en páginas viejas (~30 archivos, mecánico).
8. ⏳ **Aplicar paleta verde médica** a `MedicalRecords.tsx` y `PetClinicalRecord.tsx` (solo header tiene gradiente verde).
9. ⏳ **Renombrar `ServiceCalendar.tsx` → `MyBookings.tsx`** y ruta `/calendar` → `/mis-reservas`.
10. ⏳ **Implementar Google Calendar OAuth real** o eliminar el stub (`googleCalendar.ts` está inerte).
11. ⏳ **Splash screen iOS/Android** — Capacitor configurado pero no testeado en device real.
12. ⏳ **Push notifications nativas** (FCM/OneSignal).
13. ⏳ **Sitemap dinámico** vía Edge Function `generate-sitemap` (existe el archivo, no está deployada).
14. ⏳ **Migrar más dialogs a `ResponsiveModal`**.
15. ⏳ **Auditoría manual de coherencia visual**.
16. ⏳ **Optimizar imágenes pesadas** (PNGs > 300kB).

---

## 8. Stack técnico (sin cambios)

- **Frontend**: React 18 + TypeScript + Vite 5 + Tailwind + shadcn/ui (50+ componentes)
- **State**: TanStack React Query + React Context
- **Routing**: React Router v6 (40+ rutas, lazy loading)
- **Maps**: Leaflet + OpenStreetMap (migrado de Google Maps)
- **Backend**: Supabase (Postgres + Auth + Storage + Edge Functions Deno)
- **Auth**: Supabase Auth (Google OAuth ✅, Email/Password ✅, Facebook configurado pero no probado, Instagram FUERA DE SCOPE)
- **AI**: Claude API (Anthropic) `claude-sonnet-4-5` vía edge functions con rate limit
- **Pagos**: Webpay Plus (Transbank) — solo `webpay-confirm` activo
- **Mobile**: Capacitor 7 (Android compilable, iOS simulator)
- **Deploy web**: GitHub Pages desde `docs/` con dominio custom `pawfriend.cl`

---

## 9. Métricas del proyecto al cierre de la sesión

| Métrica | Valor |
|---|---|
| Páginas (`src/pages/`) | 33 (+1: Actividad) |
| Componentes (`src/components/`) | ~155 (+ StatusCard, ActivityFeed) |
| Hooks (`src/hooks/`) | 27 (+1: useOrganicRewards) |
| Edge functions activas | 9 (8 deployadas con rate limit + webpay) |
| Tablas Supabase | ~74 (+ pet_activities, pet_activity_cheers, ai_request_quota) |
| Líneas en `src/` | ~45.500 |
| `as any` | 0 ✅ |
| `console.*` fuera de logger | 0 ✅ |
| Errores TS (`tsc -b`) | 0 ✅ |
| Bundle index principal | 501 kB / 144 kB gzip |
| Bundle más pesado lazy | Maps (Leaflet) 89 kB / 22 kB gzip |
| Tiempo de build | ~58s |

---

## 10. Acciones manuales pendientes que el dueño tiene que hacer

1. **🔴 Spend cap Anthropic** (1 min): https://console.anthropic.com/settings/limits → USD 10/mes
2. **🟡 Eliminar `VITE_GOOGLE_MAPS_API_KEY`** + edge function `get-google-maps-key` del Supabase Dashboard (limpieza pendiente del refactor a Leaflet)
3. **🟡 Probar login + flujos críticos en device físico** (no solo browser)
4. **🟢 Borrar el worktree del rediseño** (cuando ya no haga falta):
   ```bash
   git worktree remove .claude/worktrees/agent-a4600853 --force
   ```

---

## 11. Comandos útiles

```bash
# Verificar estado git
git log --oneline -10
git status

# Build local
npm run build

# Type check (IMPORTANTE: usar tsc -b, no --noEmit, porque tsconfig raíz tiene files: [])
npx tsc -b

# Dev server
npm run dev

# Deploy edge functions
npx supabase functions deploy <nombre> --project-ref gwailbjlvevkhwcrovfd

# Aplicar migración SQL nueva (manual desde Dashboard SQL Editor, NO db push)
# Después marcar como aplicada:
# insert into supabase_migrations.schema_migrations (version, name) values ('XXX','YYY');

# Regenerar tipos Supabase
npx supabase gen types typescript --project-id gwailbjlvevkhwcrovfd > src/integrations/supabase/types.ts
```

---

## 12. Links importantes

- Dashboard Supabase: https://supabase.com/dashboard/project/gwailbjlvevkhwcrovfd
- SQL Editor: https://supabase.com/dashboard/project/gwailbjlvevkhwcrovfd/sql/new
- Edge Functions logs: https://supabase.com/dashboard/project/gwailbjlvevkhwcrovfd/functions
- GitHub Actions: https://github.com/pedrosusaetahidalgo-sudo/pet-harmony-chile/actions
- Anthropic Console: https://console.anthropic.com/settings/limits
- Producción: https://pawfriend.cl

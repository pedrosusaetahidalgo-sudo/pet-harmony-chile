# Audit end-to-end pre-hardening — 2026-04-08

> Inventario sin fixes. Cada hallazgo va a uno de los commits siguientes de la sesión.

## 1.1. Rutas vs UI de navegación

**Total rutas en `src/App.tsx`**: ~43 (incluye 8 redirects legacy).

**Bottom nav actual** (`src/components/BottomTabBar.tsx`, 5 tabs):
1. Inicio → `/home`
2. Mascotas → `/my-pets` (matchea `/pet/*`, `/edit-pet/*`, `/add-pet`)
3. Buscar vet → `/veterinarios`
4. **Mensajes** → `/chat`
5. Perfil → `/profile`

**Diferencia con el prompt**: el prompt pide la 4ª tab = **Recordatorios** (`/reminders`), no Mensajes. Decisión: en el commit `feat(nav)` reemplazar Mensajes → Recordatorios y mover el acceso a chat al drawer + Header. Si esto rompe expectativas del usuario, revertir.

**Rutas accesibles solo por deep link / drawer (huérfanas del bottom nav)**:
- `/feed`, `/actividad`, `/paw-game` (comunidad / gamificación)
- `/medical-records`, `/maps`, `/adoption`
- `/servicios`, `/services/:type`, `/mis-reservas`
- `/upgrade`, `/upgrade/success`, `/upgrade/cancel`
- `/admin`, `/settings`
- `/provider/dashboard`, `/provider/profile-edit`, `/peluquero/perfil`
- `/registro-veterinario`, `/para-veterinarios`, `/resena/:token`
- `/demo`, `/user/:userId`, `/payment-result`

Todas existen como destino de algún CTA o link interno. Pendiente: verificar 1 a 1 que cada una tenga al menos un punto de entrada UI (no se hizo en este audit por tiempo).

## 1.2. CTAs y links rotos

No se detectaron `href="#"`, `navigate("TODO")` ni rutas inventadas en `src/`. Los `Navigate` redirects en `App.tsx:98,108,112-115,125-126,136` apuntan a rutas reales.

## 1.3. Joins PostgREST sospechosos

| Archivo | Línea | Patrón | Estado FK |
|---|---|---|---|
| `src/components/admin/AdminProviders.tsx` | 37,49,61 | `service_providers → profiles:user_id(...)` | ⚠️ FK no explícita en migraciones (mismo patrón que rompió `adoption_posts`). Refactorizar a fetch separado en cliente. |
| `src/pages/MyBookings.tsx` | 36 | `service_slots → provider:provider_id(... profiles:user_id(...))` | ⚠️ Mismo riesgo. El nivel anidado `profiles:user_id` es el frágil. Separar en 2 fetches. |

## 1.4. Edge functions — error handling

Las 17 functions tienen try/catch top-level y devuelven JSON estructurado. Logging consistente con `console.error` o `logger.error`. **Sin hallazgos críticos**. Pendiente menor: estandarizar shape `{error, code, message}` (varias devuelven solo `{error}`).

## 1.5. Mobile / viewport 360×800

| # | Archivo:línea | Problema | Severidad |
|---|---|---|---|
| 1 | `src/components/EnhancedBookingDialog.tsx:308` | `w-screen` en mobile (Dialog full-width forzado) — riesgo de scroll horizontal por padding del padre | P1 |
| 2 | `src/components/CreateAdoptionPost.tsx:129` | Idem `w-screen` en mobile | P1 |
| 3 | `src/pages/Auth.tsx:383,394,446,456,467` | Inputs sin `inputMode`/`autoComplete` (email/password) | P2 |
| 4 | `src/pages/AddPet.tsx` (varios) | Inputs numéricos sin `inputMode="numeric"` (peso, edad, microchip) | P2 |
| 5 | `src/pages/Auth.tsx`, `AddPet.tsx`, `RegistroVeterinario.tsx` | Sin `scrollIntoView` en focus → keyboard tapa submit | P1 |
| 6 | `src/pages/NotFound.tsx:13-20` | 404 en inglés ("Oops! Page not found"), sin CTAs útiles | P0 |
| 7 | `src/components/provider/ProviderDashboard.tsx:75+` | Stats `0` cuando no hay reservas, sin empty state amigable | P1 |

**Bien**:
- `AppLayout.tsx:30-34` aplica `paddingBottom: calc(safe-area-bottom + 3.5rem)` → respeta safe area + altura del bottom nav.
- `BottomTabBar.tsx:70` aplica `paddingBottom: var(--safe-area-bottom)`.
- `index.css:14-17` define las 4 vars `--safe-area-*`.
- `ProtectedRoute.tsx:12-47` skeleton de app shell completo (no spinner pelado).

## 1.6. Auth/routing — checklist binario

| # | Caso | Estado |
|---|---|---|
| 1 | Usuario NO logueado entra a `/` → ve landing pública | ✅ |
| 2 | Usuario SÍ logueado entra a `/` → redirige a `/home` | ❌ `Index.tsx` no chequea `useAuth`, siempre muestra landing |
| 3 | Usuario NO logueado entra a `/my-pets` → redirige a `/auth?returnTo=/my-pets` | ✅ `ProtectedRoute.tsx:50-55` |
| 4 | Login con `returnTo` → vuelve a la ruta original | ✅ `Auth.tsx:28,42-48` |
| 5 | Logout → va a `/auth` con estado limpio | ✅ (verificado en `useAuth`) |
| 6 | 404 real → página 404 amigable | ❌ `NotFound.tsx` en inglés sin tuteo ni CTAs |
| 7 | Refresh F5 en `/pet/:id/clinical?tab=resumen` | ✅ (SPA fix `489f3b2`) |
| 8 | Deep link desde WhatsApp mobile | ✅ (mismo fix) |

**Decisión a tomar**: ¿también redirigir `/auth` → `/home` cuando el user ya está logueado? (hoy `Auth.tsx` lo deja entrar y muestra el form). Lo dejo como P1 si confirmás.

## 1.7. Dolores MVP — estado real

### B2C
- ✅ Onboarding primera mascota (`AddPet.tsx`) con validaciones edad min/max post-QA.
- ✅ Agregar vacuna en ficha clínica (5 tabs split en commit anterior).
- 🟡 Recordatorio in-app: existe `useReminders` hook + `pet_reminders` en DB, pero **no hay página `/reminders`** dedicada ni listado agregado. Hoy se ven inline en `MedicalRecords` / `Home`.
- ❌ **Compartir ficha con vet**: `LINKS.petClinicalShare` apunta a `/pet/:id/clinical?action=share` pero no hay UI evidente que genere link temporal ni botón WhatsApp. Pendiente verificación profunda.
- ✅ PDF de ficha (joya de la corona) — `@media print` arreglado en commit anterior.
- 🟡 Buscar vet por comuna: `DirectorioVets.tsx:88-99` tiene el filtro comuna en `md:grid-cols-4` — visible en desktop, pero en mobile (`grid-cols-1`) queda como cualquier otro filtro, no destacado.

### B2B Vet
- 🟡 `/para-veterinarios`: existe, pendiente revisar copy vs pitch.
- ✅ `/registro-veterinario`: existe.
- 🟡 `ProviderDashboard.tsx:75+`: cuando vacío, muestra "0" en métricas sin CTA dirigido.
- 🟡 Edición de perfil + previsualización: existe `ProviderProfileEdit` pero falta verificar botón "Ver cómo me ven los dueños".
- ✅ Contratación Flow (B2B): commits previos.
- ❌ Aviso al vet cuando dueño comparte ficha: no hay badge/contador implementado.

## 1.8. Cables sueltos / decisiones para el dueño

1. **Bottom nav 4ª tab**: ¿Recordatorios reemplaza a Mensajes? (prompt dice sí — confirmar con el dueño post-sesión)
2. **`/reminders`**: crear como página agregadora real de `pet_reminders` del usuario (no es feature nueva, solo UI sobre data existente).
3. **`webpay-confirm`**: confirmado obsoleta. ¿Borrar en esta sesión o más adelante?
4. **`/auth` con user logueado**: ¿redirigir a `/home` o dejar entrar?
5. **AdminProviders join**: solo es admin, riesgo bajo, pero conviene refactor por consistencia.

---

## Plan de fixes derivado (commits siguientes)

1. `fix(backend)`: refactor joins PostgREST `AdminProviders` + `MyBookings` + migración trigger `total_paw_points`.
2. `fix(routing)`: 404 amigable en español + `Index.tsx` redirige logueado a `/home` + `src/lib/routing.ts` con reglas documentadas.
3. `fix(mobile)`: quitar `w-screen` en 2 dialogs, `inputMode`/`autoComplete` en Auth/AddPet, `scrollIntoView` en submits.
4. `feat(nav)`: BottomTabBar tab 4 → Recordatorios + crear `/reminders` agregando `pet_reminders` + badge de vencidos + `<PageHeader>` + `<Breadcrumbs>` + empty states con CTA.
5. `fix(mvp B2C)`: botón WhatsApp share en ficha clínica, comuna filter destacado en mobile, card "próximos recordatorios" en Home.
6. `fix(mvp B2B)`: empty state ProviderDashboard, preview perfil público, badge de fichas compartidas.
7. `polish`: loading skeletons consistentes, toasts en tuteo, copy final.

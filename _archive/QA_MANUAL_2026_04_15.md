# QA Manual — Paw Friend
**Fecha**: 2026-04-15
**Tester**: Pedro
**Branch**: main (post cambios: community seed, recharts chunk, tests)
**Ambiente**: localhost:8080 (`npm run dev`)

> Marca con `[x]` los flujos OK. Si hay problema, cambia a `[!]` y escribe el detalle abajo.
> Ejemplo:
> - [x] Login con email → OK
> - [!] Login con Google → **BUG**: popup se cierra sin completar OAuth

---

## DUEÑO (owner)

### 1. Auth y Onboarding

- [ ] **Registro email/password**
  - Ir a `/auth` → tab registro → email + password 6+ chars → submit → redirect a onboarding/add-pet
  - Probar password 5 chars → error "Mínimo 6 caracteres"
  - Probar email inválido → error "Email invalido"
  > Notas:

- [ ] **Login email/password**
  - Ir a `/auth` → credenciales existentes → submit → redirect a `/home` (con mascotas) o `/add-pet`
  - Header muestra nombre/avatar
  > Notas:

- [ ] **Login Google OAuth**
  - Ir a `/auth` → botón Google → popup OAuth → completa → redirect correcto
  - Perfil creado en profiles
  > Notas:

- [ ] **Logout**
  - Desde `/profile` → "Cerrar sesión" → redirect a `/auth`
  - Navegar a `/home` sin sesión → redirect a `/auth?returnTo=/home`
  - Login → llega a `/home` (returnTo funciona)
  > Notas:

- [ ] **Onboarding mascota**
  - Post-registro → llega a `/onboarding-mascota`
  - Completar nombre + especie → submit → redirect a `/home` con mascota visible
  > Notas:

---

### 2. Home / Dashboard

- [ ] **StatusCard**
  - `/home` → nombre mascota, foto/placeholder, health score con color correcto
  - Health score: "Al día" (verde) / "Con pendientes" (ámbar) / "Requiere atención" (rojo)
  > Notas:

- [ ] **Rutinas del día**
  - TodayRoutinesCard visible con rutinas de hoy o mensaje vacío
  > Notas:

- [ ] **Accesos rápidos**
  - Click en cada acceso rápido → navega a la ruta correcta
  > Notas:

- [ ] **Selector de mascotas**
  - Si tiene múltiples mascotas → selector funciona y cambia StatusCard
  > Notas:

---

### 3. Mascotas

- [ ] **Lista mis mascotas**
  - `/my-pets` → lista muestra todas las mascotas
  - Click en mascota → navega a `/ficha/:petId`
  - Botón "Agregar mascota" → navega a `/add-pet`
  > Notas:

- [ ] **ClaimPetDialog**
  - Botón "Tengo un código de mi vet" visible en `/my-pets`
  - Click → abre dialog → código inválido → error → cerrar → vuelve a lista
  > Notas:

- [ ] **Agregar mascota**
  - `/add-pet` → nombre vacío → error "Campo requerido"
  - Especie sin seleccionar → error "Selecciona una especie"
  - Microchip "1234" → error "15 dígitos" / microchip "123456789012345" → OK / vacío → OK
  - Completar nombre + especie → submit → mascota creada → aparece en lista
  > Notas:

- [ ] **Editar mascota**
  - Desde `/my-pets` → editar → campos pre-llenados → cambiar nombre → submit → cambio reflejado
  > Notas:

---

### 4. Ficha Clínica (joya de la corona)

- [ ] **Tab Historial Médico**
  - `/ficha/:petId` → tab activo por defecto
  - Sin registros → mensaje vacío + CTA
  - Agregar registro: tipo + descripción + fecha → guardar → aparece en lista (más reciente primero)
  > Notas:

- [ ] **Tab Vacunas**
  - Click tab → catálogo sugerido para la especie si vacío
  - Agregar vacuna: nombre + fecha + lote → guardar → badge "Al día" o "Pendiente"
  > Notas:

- [ ] **Tab Documentos**
  - Click tab → "Subir documento" → seleccionar archivo → descripción → subir → aparece en lista
  - Click en documento → preview o descarga
  > Notas:

- [ ] **Tab Peso**
  - Click tab → agregar peso (valor + fecha) → punto en gráfico
  - Segundo peso → línea de tendencia visible → eje Y en kg
  > Notas:

- [ ] **Tab IA**
  - Click tab → sugerencias basadas en registros → disclaimer visible
  - Sin registros → mensaje indicando que necesita datos
  > Notas:

- [ ] **Exportar PDF**
  - Botón MedicalSummaryButton visible y clickeable
  - Click → loading → PDF se descarga → contiene: nombre, especie, raza, historial, vacunas, peso
  > Notas:

- [ ] **Exportar ZIP**
  - Botón ZIP → descarga → abrir ZIP → contiene PDF + documentos subidos
  > Notas:

- [ ] **Compartir ficha con vet**
  - Click "Compartir con vet" → ShareWithVetModal → genera link 30 días
  - Copiar link → abrir en incógnito → `/medical-share/:token` muestra ficha pública
  - No muestra datos sensibles del dueño
  > Notas:

- [ ] **OCR carnet vacunación**
  - Subir foto carnet → loading → extrae vacunas/fechas → confirmar → se guardan
  > Notas:

- [ ] **Feline Grimace Scale**
  - Mascota gato → opción visible → completar checklist → score calculado
  - Mascota perro → opción NO aparece
  > Notas:

---

### 5. Recordatorios y Calendario

- [ ] **Crear recordatorio**
  - `/reminders` → "Crear" → tipo + fecha futura + descripción → guardar → aparece en lista
  - Badge: próximo (ámbar), vencido (rojo)
  - Marcar completado → badge verde → puntos de gamificación otorgados
  > Notas:

- [ ] **Rutinas semanales**
  - `/rutinas` → crear rutina: nombre + días + hora → guardar → aparece
  - Selector de día filtra correctamente
  - `/mascota/:petId/rutinas` → solo rutinas de esa mascota
  > Notas:

- [ ] **Calendario unificado**
  - `/calendario` → vista con rutinas + recordatorios + citas
  - Click en día → UnifiedDayView con detalle
  - Colores diferencian tipo de evento
  - Navegar entre meses → datos cargan
  > Notas:

---

### 6. Comunidad (CAMBIOS HOY)

- [ ] **Carga inicial**
  - `/comunidad` → PawLabsBanner beta visible
  - Cargan los 72 grupos (si migración aplicada)
  - Contador "72 grupos" visible
  > Notas:

- [ ] **Filtros por categoría**
  - Click "Razas" → solo razas perros
  - Click "Razas Gatos" → solo razas gatos
  - Click "Salud" → solo condiciones médicas
  - Click "Regiones" → solo zonas
  - Click "Estilo de vida" → solo interés general
  - Click "Todos" → vuelven los 72
  - Click en categoría activa → se deselecciona (toggle)
  > Notas:

- [ ] **Búsqueda**
  - "golden" → muestra "Golden Retrievers Chile"
  - "santiago" → muestra "Mascotas Santiago Centro"
  - "diabetes" → muestra "Diabetes felina" y "Diabetes canina"
  - Texto sin match → "Sin resultados" + mensaje de ayuda
  - Borrar búsqueda → vuelven todos
  - Búsqueda + filtro combinados funcionan
  > Notas:

- [ ] **Iconos diferenciados**
  - breed → corazón morado / fondo purple-100
  - condition → corazón rojo / fondo red-50
  - location → pin teal / fondo teal-50
  - general → sparkles ámbar / fondo amber-50
  > Notas:

- [ ] **Unirse a grupo**
  - Click "Unirse" → botón cambia a "Salir" → redirect al chat
  - member_count incrementa
  - Volver a `/comunidad` → grupo aparece primero en lista
  - Contador muestra "X grupos · Y unidos"
  > Notas:

- [ ] **Chat grupal**
  - Header: nombre grupo + miembros
  - Escribir mensaje → burbuja morada a la derecha
  - Avatar + nombre + "hace X minutos"
  - Enter y botón Send funcionan
  - Campo vacío → Send deshabilitado
  - Sin mensajes → "Sé el primero en escribir"
  - Botón atrás → vuelve a `/comunidad`
  > Notas:

- [ ] **Salir de grupo**
  - Click "Salir" → botón cambia a "Unirse" → member_count decrementa
  - Click en card ya no navega al chat
  > Notas:

- [ ] **Grupo inexistente**
  - `/comunidad/slug-falso` → "Grupo no encontrado"
  > Notas:

---

### 7. Feed Social

- [ ] **Carga de posts**
  - `/feed` → posts cargan (infinite scroll)
  - Stories en header → click → FeedStoryViewer
  > Notas:

- [ ] **Crear post**
  - Click crear → texto + imagen → publicar → aparece en feed
  > Notas:

- [ ] **Interacciones**
  - Like → corazón se llena / doble tap → like
  - Comentar → comentario vacío bloqueado por zod → comentario válido aparece
  - Guardar post → ícono cambia
  - Reportar → 5 opciones de razón
  > Notas:

- [ ] **Feed Explore**
  - Tab Explore → trending/hashtags → búsqueda funciona
  > Notas:

---

### 8. Gamificación (owner-only)

- [ ] **PawGame**
  - `/paw-game` → RoleGuard: si modo provider → auto-switch a owner
  - GuardianProgress: nivel, XP, racha diaria
  - BadgeGallery, PawShopRewards, MissionCard visibles
  > Notas:

- [ ] **Paw Collection**
  - `/paw-collection` → Paw Cards de tus mascotas
  - Click card → flip animado → efecto holográfico por rareza
  - Swipe horizontal 60px+ → flip
  - Mobile: efecto giroscopio (tilt)
  > Notas:

- [ ] **Misiones**
  - `/misiones` → lista de misiones con progreso
  - Categorías: care, social, collection, achievement
  - Completar misión → PawPoints otorgados
  > Notas:

---

### 9. Directorio Vets (público)

- [ ] **Lista de vets**
  - `/veterinarios` → lista carga (funciona sin login)
  - Buscar por nombre → filtra
  - Filtro comuna → `/veterinarios/comuna/:comuna`
  - Filtro especialidad → `/veterinarios/especialidad/:esp`
  > Notas:

- [ ] **Perfil público vet**
  - Click vet → `/veterinarios/:slug` → bio, especialidades, horarios, reviews, mapa
  > Notas:

- [ ] **Estimador de precios**
  - `/precios-veterinarios` → seleccionar comuna → rango de precios coherente
  > Notas:

---

### 10. Upgrade y Pagos

- [ ] **Página upgrade**
  - `/upgrade` → tabla de planes visible
  - Si USER_PREMIUM=false → app funciona sin gates
  - Si USER_PREMIUM=true → click Upgrade → redirect a Flow.cl
  > Notas:

- [ ] **Resultado pago**
  - `/upgrade/success` → mensaje éxito
  - `/upgrade/cancel` → mensaje cancelación
  - `/payment-result?status=success` → OK
  - `/payment-result?status=failed` → error
  > Notas:

---

### 11. Otros flujos owner

- [ ] **Mapa de servicios**
  - `/maps` → Leaflet carga (chunk lazy) → pins visibles → click pin → popup
  - MapFilters filtran por tipo
  > Notas:

- [ ] **Chat**
  - `/chat` → lista conversaciones → click → `/chat/:id` → enviar mensaje → realtime
  > Notas:

- [ ] **Perfil**
  - `/profile` → datos correctos → editar → guardar → cambios reflejados
  - ProfileCompletionCard: % completitud
  > Notas:

- [ ] **Memorial**
  - `/en-memoria` → crear memorial → BereavementChat responde → teléfonos crisis visibles
  > Notas:

- [ ] **Adopción**
  - `/adoption` → PawLabsBanner → lista posts → mostrar interés
  > Notas:

- [ ] **Donantes sangre**
  - `/donantes-sangre` → PawLabsBanner → lista/formulario
  > Notas:

- [ ] **Reportes**
  - `/reportes` → resumen semanal → gráficos Recharts cargan (lazy)
  > Notas:

- [ ] **Servicios**
  - `/servicios` → lista → click tipo → `/services/:type` → filtros y búsqueda
  > Notas:

---

## PROVIDER (veterinario)

### 12. Registro y Onboarding

- [ ] **Landing B2B**
  - `/para-veterinarios` → contenido + planes + CTA "Registrarme" → navega a `/registro-veterinario`
  > Notas:

- [ ] **Registro veterinario**
  - Nombre 2 chars → error "al menos 3" / nombre 3+ → OK
  - Email inválido → error / password 5 chars → error "Mínimo 6"
  - Formulario válido + docs → submit → "en revisión"
  > Notas:

- [ ] **Onboarding vet**
  - `/onboarding-vet` → displayName 2 chars → error
  - providerType: individual/clinic/home_visit → OK
  - Campos opcionales: comuna, especialidades, bio max 500
  - Submit → redirect a `/provider/dashboard`
  > Notas:

---

### 13. Dashboard Provider

- [ ] **Métricas**
  - `/provider/dashboard` → 6 métricas en cards
  - InteractiveMetricCard: click → detalle con mini-gráfico
  > Notas:

- [ ] **Tab Clínico**
  - Últimas consultas, AlertsBanner, QuickActionsBar
  > Notas:

- [ ] **Tab Negocio (Recharts — CAMBIO HOY)**
  - AreaChart revenue/bookings renderiza sin errores
  - Datos coherentes
  > Notas:

- [ ] **Tab Pacientes**
  - Pacientes recientes → click → navega al detalle
  > Notas:

- [ ] **ActivityFeed + Next24hCard**
  - Actividad reciente visible
  - Citas próximas 24h visibles
  > Notas:

---

### 14. Pacientes

- [ ] **Lista pacientes**
  - `/provider/pacientes` → lista carga → buscar por nombre mascota/dueño → filtra
  > Notas:

- [ ] **Crear paciente (mascota huérfana)**
  - NewPatientForm → owner_name 1 char → error "al menos 2"
  - owner_email inválido → error
  - microchip "abc" → error "15 dígitos"
  - Completar nombre + especie + owner_name + owner_email → submit
  - Mascota creada con owner_id=null
  - Email de invitación enviado (send-pet-invitation)
  > Notas:

- [ ] **Resumen consolidado IA**
  - Detalle paciente → "Resumen IA" → loading → resumen coherente en español chileno
  > Notas:

---

### 15. Perfil Provider

- [ ] **Editar perfil**
  - `/provider/profile-edit` → campos pre-llenados → editar bio → guardar → cambio reflejado
  > Notas:

- [ ] **Ver como me ven los dueños**
  - Click preview → vista pública → coincide con `/veterinarios/:slug`
  > Notas:

- [ ] **Horarios de atención**
  - Editar horarios → guardar → verificar en perfil público
  > Notas:

---

### 16. Herramientas Clínicas

- [ ] **Quick Notes**
  - Crear nota rápida para paciente → se guarda → aparece en lista → incluida en resumen consolidado
  > Notas:

- [ ] **Plantillas post-consulta**
  - Abrir ClinicalNoteEditor → seleccionar plantilla → campos pre-llenan → editar → guardar
  > Notas:

- [ ] **Grabación audio consulta**
  - Iniciar grabación → detener → transcripción IA → texto coherente
  > Notas:

---

### 17. Cambio de Rol (dual-role)

- [ ] **Toggle en Header**
  - Toggle "Dueño / Profesional" visible si es dual-role
  - Click "Dueño" → redirect `/home` + toast
  - Click "Profesional" → redirect `/provider/dashboard` + toast
  > Notas:

- [ ] **Menús por rol**
  - Modo dueño: BottomTabBar/Sidebar muestra Home, Feed, Mis mascotas, etc. NO provider/*
  - Modo provider: muestra Dashboard, Pacientes, Perfil. NO paw-game, misiones, paw-collection
  > Notas:

- [ ] **RoleGuard auto-switch**
  - Modo provider → navegar a `/paw-game` → auto-switch a owner → carga la página
  - Modo owner → navegar a `/provider/dashboard` → auto-switch a provider → carga
  > Notas:

---

## ADMIN

### 18. Panel Admin

- [ ] **Acceso**
  - `/admin` → requiere admin_access is_active=true
  - No admin → redirect o error
  > Notas:

- [ ] **Dashboard (Recharts — CAMBIO HOY)**
  - KPIs: usuarios activos 7d, revenue, providers pendientes
  - AreaChart registros 30d + BarChart mascotas 30d renderizan sin errores
  > Notas:

- [ ] **Finance**
  - Órdenes y suscripciones + gráficos BarChart/PieChart
  > Notas:

- [ ] **Users**
  - Lista con búsqueda → click → detalle con mascotas y actividad
  > Notas:

- [ ] **Providers**
  - Lista con estado → aprobar/rechazar → estado cambia
  > Notas:

- [ ] **Moderation**
  - Reportes pendientes → aprobar/eliminar contenido
  > Notas:

- [ ] **Missions + Rewards**
  - CRUD misiones + sistema recompensas
  > Notas:

- [ ] **ErrorLog**
  - Errores frontend + gráfico por día (BarChart)
  > Notas:

- [ ] **SystemHealth + AuditLog + Team**
  - Health logs, acciones admin, lista admins
  > Notas:

- [ ] **PendingPets**
  - Mascotas sin dueño → muestra pending_owner_email
  > Notas:

---

## CROSS-CUTTING

### 19. Performance (CAMBIOS HOY)

- [ ] **Lazy loading chunks**
  - `/home` → DevTools Network: recharts-vendor NO descargado, leaflet-vendor NO descargado
  - sentry-vendor SÍ descargado (se usa al inicio)
  - Navegar a `/provider/dashboard` → recharts-vendor se descarga
  - Navegar a `/maps` → leaflet-vendor se descarga
  - Segunda visita → chunks cacheados (no re-descarga)
  > Notas:

### 20. Redirects legacy

- [ ] `/settings` → redirect a `/profile`
- [ ] `/calendar` → redirect a `/mis-reservas`
- [ ] `/mascota/123/ficha-clinica` → redirect a `/ficha/123`
- [ ] `/pet/123/clinical` → redirect a `/ficha/123`
> Notas:

### 21. 404

- [ ] `/ruta-que-no-existe` → página NotFound con CTAs → sin crash ni pantalla blanca
> Notas:

### 22. Responsive mobile

- [ ] DevTools responsive 375px
- [ ] `/home` → cards apiladas verticalmente
- [ ] `/comunidad` → filtros scroll horizontal
- [ ] `/ficha/:petId` → tabs funcionan en mobile
- [ ] BottomTabBar visible y funcional
- [ ] Sidebar oculta → hamburger menu la abre
> Notas:

### 23. Dark mode

- [ ] Toggle dark mode funciona
- [ ] Colores correctos en: Home, Feed, Ficha, Comunidad, Dashboard provider
- [ ] No hay texto invisible (claro sobre claro)
- [ ] Iconos comunidad visibles en dark
- [ ] Gráficos Recharts: ejes y labels legibles
> Notas:

### 24. Landing pública

- [ ] `/` sin login → HeroV2 + MedicalPDFShowcase + 8 secciones
- [ ] CTAs "Registrarme" y "Soy Veterinario" navegan correctamente
- [ ] `/` con login → redirect a `/home`
> Notas:

---

## RESUMEN

| Área | Total flujos | OK | Bugs | Pendiente |
|---|---|---|---|---|
| Auth y Onboarding | 5 | | | |
| Home/Dashboard | 4 | | | |
| Mascotas | 4 | | | |
| Ficha Clínica | 11 | | | |
| Recordatorios/Calendario | 3 | | | |
| Comunidad | 7 | | | |
| Feed Social | 4 | | | |
| Gamificación | 3 | | | |
| Directorio Vets | 3 | | | |
| Upgrade/Pagos | 2 | | | |
| Otros owner | 8 | | | |
| Provider registro | 3 | | | |
| Provider dashboard | 5 | | | |
| Provider pacientes | 3 | | | |
| Provider perfil | 3 | | | |
| Herramientas clínicas | 3 | | | |
| Cambio de rol | 3 | | | |
| Admin | 10 | | | |
| Cross-cutting | 6 | | | |
| **TOTAL** | **89** | | | |

---

## BUGS ENCONTRADOS

> Copiar aquí cada bug encontrado con formato:
>
> ### BUG-001: [Título corto]
> - **Flujo**: #X — nombre del flujo
> - **Severidad**: Crítico / Alto / Medio / Bajo
> - **Pasos para reproducir**:
>   1. Paso 1
>   2. Paso 2
> - **Resultado esperado**: ...
> - **Resultado actual**: ...
> - **Screenshot**: (pegar path si hay)
> - **Navegador/Device**: Chrome 126 / mobile 375px / etc.

---

## MEJORAS DETECTADAS

> Cosas que no son bugs pero podrían mejorar:
>
> ### MEJORA-001: [Título corto]
> - **Flujo**: #X
> - **Descripción**: ...
> - **Prioridad sugerida**: Alta / Media / Baja

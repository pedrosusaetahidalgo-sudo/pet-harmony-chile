# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (NEVER commit secrets)
.env*
!.env.example

# Mi documento personal de API keys (NUNCA commitear)
MIS_API_KEYS.md
MIS_API_KEYS.local.md

# Backup files
*.bak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.app.tsbuildinfo
tsconfig.node.tsbuildinfo

# Claude Code (ignorar todo excepto agentes compartidos)
.claude/
!.claude/agents/

# Scripts personales locales del dueño (no compartir en el repo)
_clean.ps1
_diag.ps1

# Playwright
playwright-report/
test-results/
blob-report/
e2e/.auth/

# Bundle visualizer
stats.html

# Performance reports (Lighthouse)
perf-reports/

# Legacy junk folder (archivos pre-Claude, sin valor accionable)
junk/

# Android signing (NEVER commit)
*.keystore
*.jks
keystore.properties
android/app/google-services.json

# iOS signing & Firebase (NEVER commit)
ios/App/App/GoogleService-Info.plist
*.mobileprovision
*.p12
*.cer
